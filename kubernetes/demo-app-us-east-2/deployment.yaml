apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
  namespace: demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demo-app
  template:
    metadata:
      labels:
        app: demo-app
    spec:
      nodeSelector:
        karpenter.sh/nodepool: default
      containers:
        - name: demo-app
          image: public.ecr.aws/nginx/nginx:latest
          ports:
            - containerPort: 80
